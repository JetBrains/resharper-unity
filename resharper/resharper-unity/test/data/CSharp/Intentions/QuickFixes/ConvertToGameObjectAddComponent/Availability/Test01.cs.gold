using UnityEngine;

public class Foo : MonoBehaviour
{
    public static Foo DoSomething()
    {
        var foo = |new Foo()|(0);
        return foo;
    }
}

------------------------------------------------
0: MonoBehaviours must be instantiated with 'GameObject.AddComponent<T>()' instead of 'new'
QUICKFIXES:
Convert to 'GameObject.AddComponent<Foo>()'
