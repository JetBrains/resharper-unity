using UnityEngine;

namespace DefaultNamespace
{
    public class InlinedRestoreTest
    {
        public void Test(Transform t)
        {
          var position = t.position;
          position = position + pos{caret}ition;
            t.position = position + position + (t.position = Vector3.Back) + t.localPosition;
        }
    }
}