using UnityEngine;

namespace DefaultNamespace
{
    public class MultiReplace : MonoBehaviour
    {
        public void Update()
        {
            var transform = GetCo{caret}mponent<Transform>();

            var t = transform.position.x + transform.position.x + transform.position.z;
        }
    }
}