using UnityEngine;

namespace Test
{
    public class TestClass : MonoBehaviour
    {
        public void Update()
        {
            var transform = GetComponent<Transform>();
            var istransformNull = transform == null;
            for (int i = 0; i < 100; i++) 
            {
                if (istransformN{caret}ull)
                {

                }
            }
        }
    }
}