using UnityEngine;

public class Foo : ScriptableObject
{
    public static Foo DoSomething()
    {
        var foo = |new Foo()|(0);
        return foo;
    }
}

------------------------------------------------
0: ScriptableObjects must be instantiated with 'ScriptableObject.CreateInstance<T>()' instead of 'new'
QUICKFIXES:
Convert to 'ScriptableObject.CreateInstance<Foo>()'
